{
  "name": "justreadcomics",
  "version": "1.0.0",
  "license": "MIT",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "start:dev": "lerna run --parallel --stream start:dev",
    "start:build-run-prod": "lerna run --parallel --stream start:build-run-prod",
    "start": "lerna run --parallel --stream start",
    "build": "lerna run --parallel --stream build"
  },
  "devDependencies": {
    "lerna": "^7.4.2",
    "nx": "17.2.6"
  },
  "dependencies": {
    "bootstrap": "^5.3.2",
    "react-bootstrap": "^2.9.1"
  }
}
