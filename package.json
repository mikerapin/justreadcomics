{
  "name": "justreadcomics",
  "license": "MIT",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "start": "lerna run --parallel --stream start",
    "build": "lerna run --parallel --stream build"
  },
  "dependencies": {},
  "devDependencies": {
    "lerna": "^7.4.2"
  }
}
