{
  "name": "justreadcomics",
  "version": "1.0.0",
  "license": "MIT",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "common": "nx build @justreadcomics/common --output-style=static && nx build @justreadcomics/shared-node --output-style=static",
    "start:dev": "npm run common && nx run-many -t start:dev --output-style=static --exclude @justreadcomics/common @justreadcomics/shared-node",
    "start:build-run-prod": "npm run common && nx run-many -t start:build-run-prod",
    "start": "nx run-many -t start",
    "build": "npm run common && nx run-many -t build --exclude @justreadcomics/common @justreadcomics/shared-node"
  },
  "devDependencies": {
    "lerna": "^7.4.2",
    "nx": "17.2.6"
  },
  "dependencies": {
    "bootstrap": "^5.3.2",
    "react-bootstrap": "^2.9.1"
  }
}

